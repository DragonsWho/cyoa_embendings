source venv/bin/activate
rm games.index chunk_map.json
 
python indexer.py

 
uvicorn main:app --reload --port 8100

 Конечно, спокойной ночи! Отличная идея — зафиксировать текущий статус. Это поможет нам завтра быстро вернуться в контекст.

Я полностью переписал `README.md`, чтобы он отражал текущее состояние проекта, проделанную работу и наши дальнейшие планы.

---

# README.md (Новая версия)

# Семантический поиск для каталога CYOA.cafe

Этот проект представляет собой Python-бэкенд для реализации семантического (смыслового) поиска по каталогу игр [CYOA.cafe](https://cyoa.cafe/). Система позволяет находить игры по нечетким, описательным запросам пользователя, даже если он не помнит точных названий.

Основная идея — автоматически извлекать полные тексты игр с их оригинальных хостингов, разбивать их на небольшие фрагменты (чанки), преобразовывать в числовые векторы (эмбеддинги) с помощью Google Gemini API и индексировать их для молниеносного поиска с помощью библиотеки Faiss.

## Ключевые особенности и архитектура

*   **Интеграция с PocketBase:** Система полностью интегрирована с основной базой данных каталога. Метаданные игр (названия, ссылки на оригиналы) синхронизируются в локальную базу данных.
*   **Автоматический краулер текстов:** Мощный скрипт (`fetch_game_text.py`) находит игры без текста, переходит по ссылке на оригинал и использует многоуровневую стратегию (прямой запрос, анализ трафика, инспекция JS) для извлечения полного текста игры.
*   **Современная модель эмбеддингов:** Используется `gemini-embedding-001` от Google для преобразования текста в векторы.
*   **Эффективное индексирование (MRL + PQ):**
    1.  **Matryoshka Representation Learning (MRL):** Запрашиваются короткие, но качественные 256-мерные векторы, что в 4 раза сокращает объем данных "на старте".
    2.  **Faiss `IndexIVFPQ`:** Векторы дополнительно сжимаются и кластеризуются. Это обеспечивает минимальный размер поискового индекса и сверхбыстрый поиск.
*   **Надежная архитектура:** Процессы синхронизации, извлечения текста и индексации разделены, что позволяет запускать их независимо и обеспечивает отказоустойчивость.
*   **Веб-интерфейс:** Простой UI на HTML/CSS/JS, который обслуживается FastAPI. Позволяет тестировать поиск, просматривать статистику и список проиндексированных игр.

## Структура проекта

```
/
|-- static/                  # Файлы для фронтенда (UI)
|   |-- index.html
|   |-- style.css
|   `-- script.js
|
|-- .env                     # Хранит GOOGLE_API_KEY и учетные данные PocketBase
|-- create_database.py       # (Одноразово) Создает схему БД
|-- sync_with_pocketbase.py  # Синхронизирует метаданные игр из PocketBase
|-- fetch_game_text.py       # Скачивает и извлекает тексты игр с их сайтов
|-- indexer.py               # Создает/обновляет поисковый индекс
|-- reset_index_status.py    # Утилита для сброса статуса индексации всех игр
|-- games.db                 # Локальная база данных SQLite
|-- main.py                  # FastAPI сервер (API и UI)
`-- requirements.txt         # Зависимости проекта
```

---
## Основной рабочий цикл (Workflow)

### 1. Синхронизация с каталогом
Забираем свежие метаданные (ID, название, URL оригинала) из PocketBase в локальную базу.
```bash
python sync_with_pocketbase.py
```

### 2. Извлечение текстов
Скрипт находит игры без текста и пытается скачать их с оригинальных сайтов. Это может быть долгий процесс.
```bash
python fetch_game_text.py
```

### 3. Индексация
Скрипт находит все новые тексты, генерирует эмбеддинги и обновляет поисковый индекс.
```bash
python indexer.py
```

### 4. Запуск сервера
Запускаем API и веб-интерфейс для тестирования.
```bash
uvicorn main:app --reload --port 8100
```
После этого можно открыть `http://127.0.0.1:8100/` в браузере.

### Полная переиндексация (начать всё заново)
Иногда нужно перестроить весь индекс с нуля.

1.  **Остановить сервер** (`Ctrl+C`).
2.  **Сбросить статусы в базе данных:**
    ```bash
    python reset_index_status.py
    ```
3.  **Удалить старые файлы индекса:**
    ```bash
    # macOS / Linux:
    rm games.index chunk_map.json
    # Windows:
    del games.index chunk_map.json
    ```
4.  **Запустить индексатор `python indexer.py`** для создания нового, чистого индекса.

---
## Текущий статус проекта и следующие шаги

### Что сделано и работает:
✅ **Полная архитектура:** Созданы и связаны все ключевые компоненты: синхронизатор, краулер, индексатор и API.
✅ **Синхронизация с PocketBase:** Скрипт `sync_with_pocketbase.py` успешно получает метаданные игр, включая ключевую ссылку на оригинал (`iframe_url`).
✅ **Извлечение текста:** Скрипт `fetch_game_text.py` успешно обрабатывает игры, используя многоуровневую стратегию, и сохраняет тексты в базу.
✅ **Индексация:** Скрипт `indexer.py` надежно обрабатывает тексты, генерирует эмбеддинги (с учетом лимитов API) и строит поисковый индекс Faiss.
✅ **Базовый UI:** Интерфейс показывает статистику, список игр и позволяет выполнять поиск.

### На чём мы остановились (ключевая проблема):
🔴 **Качество поиска и калибровка релевантности.** Несмотря на то, что технически поиск работает, его результаты пока неинтуитивны:
- **Всегда высокая релевантность:** Даже для бессмысленных запросов система возвращает результаты с оценкой ~93-94%.
- **Малый разброс оценок:** Разница в релевантности между очень хорошим и случайным результатом составляет всего 1-2 процентных пункта.

**Гипотеза:** Проблема кроется в калибровке оценки релевантности. Текущие попытки исправить это (изменение формулы расчета score и введение порога отсечения в `main.py`) пока не дали ожидаемого эффекта. Возможно, проблема глубже — в самой природе L2-расстояния для нормализованных векторов Gemini.

### План на следующую сессию:
1.  **Глубокая диагностика поиска:**
    -   Вывести в лог или в API-ответ сырые значения L2-расстояния (`dist`), которые возвращает Faiss, для хороших и плохих запросов. Это позволит нам понять реальный диапазон значений.
    -   Проверить, не нужно ли нам использовать другую метрику вместо L2-расстояния. Faiss поддерживает **скалярное произведение (Inner Product)**, которое лучше работает для нормализованных векторов (а эмбеддинги Gemini нормализованы). Это может быть ключевым решением.
    
2.  **Переход на `IndexFlatIP` (скалярное произведение):**
    -   Модифицировать `indexer.py` и `main.py` для использования `faiss.IndexFlatIP` вместо `faiss.IndexFlatL2`.
    -   Это потребует нормализации векторов перед добавлением в индекс (хотя они уже должны быть нормализованы).
    -   Формула расчета score также изменится (для скалярного произведения чем больше значение, тем лучше).

3.  **Тюнинг агрегации результатов (To-Do из заметок):**
    -   Реализовать более продвинутую логику подсчета очков. Вместо того чтобы брать только лучший чанк для игры, можно попробовать суммировать очки нескольких релевантных чанков одной игры или учитывать их количество. Это повысит релевантность игр, в которых тема запроса упоминается многократно.














"Workflow для полной переиндексации"
Теперь у тебя есть четкий и безопасный процесс:
Останови веб-сервер (Ctrl+C в терминале, где запущен uvicorn).
Сбрось статусы в базе данных:

python reset_index_status.py
(Нажми y для подтверждения)
Удали старые файлы индекса:

rm games.index chunk_map.json  # или `del` в Windows

Запусти индексатор:

python indexer.py

Он найдет все игры с текстом (потому что мы сбросили их статус), сгенерирует для них эмбеддинги и создаст совершенно новые games.index и chunk_map.json.
Запусти веб-сервер:

uvicorn main:app --reload

Сервер подхватит свежие файлы индекса, и твой поиск будет работать на самых полных и актуальных данных.
Этот процесс можно повторять каждый раз, когда ты хочешь быть на 100% уверен, что поисковая база в идеальном состоянии.