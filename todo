добавить извлечение текста из статичных CYOA

в промте выделить основные разделы

внешний вид?



    "Full",            # Лучше использовать "Demo" для демо-версий, а "Full" - состояние по умолчанию
"English",         # Если 99% контента на английском, этот тег не нужен
 

    

     




DELETE

    "Relationship Building": ["Dating Sim", "Marriage", "Romantic"], # Объединяем в механику, а не жанр
    "Resource Management": ["Monthly Income"], Resource
    
    "Point System": ["Point Buy System"], # Уже есть в защищенных, но на всякий случай
     











Bunch of Endings'

   - ❌ Ошибка: Главный тег 'Male POV' не найден в базе. Пропускаю правило.

▶️ Обрабатываю правило: объединить все в 'Female POV'
   - ❌ Ошибка: Главный тег 'Female POV' не найден в базе. Пропускаю правило.

▶️ Обрабатываю правило: объединить все в 'Futa POV'
   - ❌ Ошибка: Главный тег 'Futa POV' не найден в базе. Пропускаю правило.

▶️ Обрабатываю правило: объединить все в 'Villain POV'
   - ❌ Ошибка: Главный тег 'Villain POV' не найден в базе. Пропускаю правило.

▶️ Обрабатываю правило: объединить все в 'Monster POV'
   - ❌ Ошибка: Главный тег 'Monster POV' не найден в базе. Пропускаю правило.